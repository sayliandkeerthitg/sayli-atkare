#include<stdio.h>
int main()
{
int a,b,c,i,j,k,size;
printf("enter the size");
scanf("%d\n%d\%d",&a,&b,&c);
int size[a][b][c];
for(i=0;i<=size;i++)
{
    for(j=0;i<size;i++)
    {
        for(k=0;k<=1;k++)
        {
            scanf("%d",&a[i][j][k]);
        }}}
        for(i=0;i<=size;i++)
{
    for(j=0;i<size;i++)
    {
        for(k=0;k<=1;k++)
        {
            printf("%d",size[a][b][c]);
        }
    }
}
}
